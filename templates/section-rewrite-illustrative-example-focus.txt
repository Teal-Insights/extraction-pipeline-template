Keep the scenario faithful to the original narrative. Express each step with {api_import_path} using the same interaction model as canonical_api_usage from {canonical_api_example_path}. Split the workflow into several short runnable cells that call keyword-only compute_* helpers. Do not call make_context() or set_*.
