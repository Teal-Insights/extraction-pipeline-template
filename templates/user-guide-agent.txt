You are writing economist-facing documentation for {library_name}, a Python
reimplementation of an Excel workbook guidance note.

Facts (not style rules):
- Package import path: `{api_import_path}`
- Install command: `{install_command}`
- Guidance note: `docs-source/guidance-note.md`
- Generated package modules live under `{package_name}/` (especially `api.py`)
- Interactive series graph (FormulaEvaluator): `assets/graph/`,
  `{package_name}/graph_api.py`, `scripts/serve_graph_api.py`
  (see `assets/graph/README.md` and `templates/series-graph/docs/` in the
  extraction pipeline)

Your job:
1. Read the guidance note and explore the public API by reading code and
   running experiments with `uv run --project . python`.
2. Inspect `user_guide/` before writing. Existing pages are a starting point:
   update, reorganize, or clean them up when they still help an economist use
   this library. Add or remove pages only as needed. Do not rebuild from scratch
   unless the current pages cannot be salvaged. Choose page count, titles, and
   narrative freely.
3. On the landing page (`user_guide/index.qmd` or equivalent), include a short
   **Interactive dependency graph** section that embeds `assets/graph/` (iframe
   preview + link to the fullscreen explorer). Declare Quarto
   `resources: [../assets/graph/**]`. Do **not** write a long topology blurb
   under that heading — the viz is the explanation. Follow
   `templates/series-graph/docs/user-guide-graph-section.md` in the extraction
   pipeline repo for structure/CSS.
4. Use Quarto runnable fences exactly as ` ```{{python}} ` for executable
   examples (not plain ` ```python `).
5. Do not modify generated package modules under `{package_name}/`. You may
   add documentation-only dependencies to `pyproject.toml`.
6. Do not create or overwrite `user_guide/99-excel-parity-validation.qmd`; the
   pipeline writes that page after you finish. You may link to
   `user-guide/excel-parity-validation.qmd` from a landing page.
7. Render the docs with this exact command and iterate until it succeeds:

```bash
uv run --project . --with great-docs great-docs build --project-path .
```

Stop when `user_guide/` has useful pages and that build succeeds.
